---
name: my-skill-name
description: >-
  Write a third-person trigger description here. State clearly WHEN this skill
  should be activated (e.g., "Use when the user asks to analyze a poker hand
  history") AND when it should NOT be used (e.g., "Do not use for general math
  questions or non-poker card games"). A strong description lets Claude Code
  match user intent accurately without false positives.
---

## Procedure

1. Step one of what the skill does.
2. Step two.
3. Continue as needed.

> Reference bundled files with `${CLAUDE_PLUGIN_ROOT}/path/to/file` rather than
> absolute paths. This keeps the skill portable across machines and clones.

## Notes

- Add any caveats, edge cases, or tips here.
