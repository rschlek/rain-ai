# inbox.md - capture stream

The append-only intake for the brain. Captures land here as timestamped, provenance-tagged
entries (schema in `RULEBOOK.md`). This is untrusted staging: nothing here is canonical until a
refine pass synthesizes it into wiki pages under the user's review. Entries are permanent -
even after refine consumes one it stays as provenance. Append new entries at the bottom; never
reorder or rewrite existing ones.

The watermark marks how far refine has synthesized. Everything beneath it is pending.

<!-- synthesized through: (nothing yet) -->
